1️⃣ What is the difference between var, let, and const?

var: smae variable can redeclare
let: variable can not redeclare but we can update the variable.
const : variable can not redeclear and can not update variable.

2️⃣ What is the spread operator (...)?

we can easyle copy array and object using spread operator

3️⃣ What is the difference between map(), filter(), and forEach()?

map(): it is work on every element in array and return a new array
filter():it work on array with spacific condition and return a new array 
forEach():it is use only for looping but do not return any array

4️⃣ What is an arrow function?

it come from ES6 this is the short syntex to write a function

5️⃣ What are template literals?

template literals is the way to write multi line string Using backticks and ${} this formate to create a daunamic website template literals 
is very importent